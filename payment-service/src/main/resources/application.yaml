spring:
  application:
    name: payment-service

  profiles:
    active: local

  jpa:
    hibernate:
      ddl-auto: update

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /payment-swagger-ui.html
    enabled: true
    doc-expansion: list
    operations-sorter: alpha
