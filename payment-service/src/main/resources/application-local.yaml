spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/payment_db
    username: postgres
    password: password

  security:
    user:
      name: user
      password: password

  kafka:
    bootstrap-servers: localhost:9092
    listener:
      auto-startup: false

  docker:
    compose:
      enabled: false
