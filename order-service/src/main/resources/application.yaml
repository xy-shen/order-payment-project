spring:
  application:
    name: order-service

  profiles:
    active: local

server:
  port: 8080

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /order-swagger-ui.html
    enabled: true
    doc-expansion: list
    operations-sorter: alpha